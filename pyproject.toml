[project]
name = "mcp-ssh-server"
version = "0.1.0"
description = "MCP server that runs remote commands over SSH."
requires-python = ">=3.10"
dependencies = [
  "mcp>=1.0.0",
  "asyncssh>=2.14.0"
]

[project.scripts]
mcp-ssh-server = "mcp_ssh_server:main"
